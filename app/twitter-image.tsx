// X/Twitter large-summary card — reuses the same generated share image as
// Open Graph so both networks show an identical, on-brand preview.
export { default, alt, size, contentType } from "./opengraph-image";
